/**
 * Auto-generated by split_recomp_by_object.py
 * Object source: libraries/source/zero/zeroutils.c
 * Functions: 12
 */

#define RECOMP_GENERATED_CODE
#include "recomp_funcs.h"
#include <math.h>

static int zeroutils_va_is_valid(uint32_t va)
{
    return va >= 0x10000u && va < 0x80000000u;
}

static unsigned char zeroutils_ascii_lower(unsigned char ch)
{
    return (ch >= 'A' && ch <= 'Z') ? (unsigned char)(ch + ('a' - 'A')) : ch;
}

static uint32_t zeroutils_stristr_host(uint32_t haystack_va, uint32_t needle_va)
{
    const char *haystack;
    const char *needle;
    size_t hay_len = 0;
    size_t needle_len = 0;

    if (!zeroutils_va_is_valid(haystack_va) || !zeroutils_va_is_valid(needle_va))
        return 0;

    haystack = (const char *)XBOX_PTR(haystack_va);
    needle = (const char *)XBOX_PTR(needle_va);

    while (hay_len < 4096 && haystack[hay_len])
        hay_len++;
    while (needle_len < 4096 && needle[needle_len])
        needle_len++;

    if (needle_len == 0)
        return haystack_va;
    if (hay_len == 4096 || needle_len == 4096 || needle_len > hay_len)
        return 0;

    for (size_t i = 0; i + needle_len <= hay_len; i++) {
        size_t j;
        for (j = 0; j < needle_len; j++) {
            if (zeroutils_ascii_lower((unsigned char)haystack[i + j]) !=
                zeroutils_ascii_lower((unsigned char)needle[j])) {
                break;
            }
        }
        if (j == needle_len)
            return haystack_va + (uint32_t)i;
    }

    return 0;
}

static uint32_t zeroutils_wcsistr_host(uint32_t haystack_va, uint32_t needle_va)
{
    size_t hay_len = 0;
    size_t needle_len = 0;

    if (!zeroutils_va_is_valid(haystack_va) || !zeroutils_va_is_valid(needle_va))
        return 0;

    while (hay_len < 4096 && MEM16(haystack_va + (uint32_t)(hay_len * 2)) != 0)
        hay_len++;
    while (needle_len < 4096 && MEM16(needle_va + (uint32_t)(needle_len * 2)) != 0)
        needle_len++;

    if (needle_len == 0)
        return haystack_va;
    if (hay_len == 4096 || needle_len == 4096 || needle_len > hay_len)
        return 0;

    for (size_t i = 0; i + needle_len <= hay_len; i++) {
        size_t j;
        for (j = 0; j < needle_len; j++) {
            uint16_t hc = MEM16(haystack_va + (uint32_t)((i + j) * 2));
            uint16_t nc = MEM16(needle_va + (uint32_t)(j * 2));
            if (hc >= 'A' && hc <= 'Z') hc = (uint16_t)(hc + ('a' - 'A'));
            if (nc >= 'A' && nc <= 'Z') nc = (uint16_t)(nc + ('a' - 'A'));
            if (hc != nc)
                break;
        }
        if (j == needle_len)
            return haystack_va + (uint32_t)(i * 2);
    }

    return 0;
}

/**
 * fn_00371BA0_wcsistr
 * Symbol: ?wcsistr@@YAPBGPBG0@Z
 * Original: 0x00371BA0 - 0x00371BAF (15 bytes, 9 insns)
 * CC: cdecl, 0 params, returns int_or_void
 * Frame: fpo_leaf
 */
void fn_00371BA0_wcsistr(void)
{
    int _flags = 0; /* fallback flag var */

    eax = zeroutils_wcsistr_host(eax, MEM32(esp + 4));
    esp += 4; return;

loc_00371BA0:
    PUSH32(esp, ebx);
    PUSH32(esp, esi);
    esi = MEM32(esp + 0xC);
    (void)0; /* test esi, esi - flags set for next jcc */
    ebx = eax;
    if (!zeroutils_va_is_valid(esi) || !zeroutils_va_is_valid(ebx)) {
        eax = 0;
        POP32(esp, esi);
        POP32(esp, ebx);
        esp += 4; return;
    }
    if (TEST_NZ(esi, esi)) { sub_00371BAF(); return; } /* jne: not equal / not zero */

loc_00371BAC:
    POP32(esp, esi);
    POP32(esp, ebx);
    esp += 4; return; /* ret */

}

/**
 * sub_00371BAF
 * Original: 0x00371BAF - 0x00371BB8 (9 bytes, 6 insns)
 * CC: cdecl, 0 params, returns int_or_void
 * Frame: fpo_leaf
 */
void sub_00371BAF(void)
{
    int _flags = 0; /* fallback flag var */

loc_00371BAF:
    if (TEST_NZ(ebx, ebx)) { sub_00371BB8(); return; } /* jne: not equal / not zero */

loc_00371BB3:
    POP32(esp, esi);
    eax = 0; /* xor self */
    POP32(esp, ebx);
    esp += 4; return; /* ret */

}

/**
 * sub_00371BB8
 * Original: 0x00371BB8 - 0x00371C02 (74 bytes, 30 insns)
 * CC: cdecl, 0 params, returns int_or_void
 * Frame: fpo_leaf
 */
void sub_00371BB8(void)
{
    uint32_t ebp;
    int _flags = 0; /* fallback flag var */
    ebp = g_seh_ebp; /* fpo_leaf: inherit caller's frame */

loc_00371BB8:
    (void)0; /* cmp MEM16(ebx), 0 - flags set for next jcc */
    PUSH32(esp, ebp);
    PUSH32(esp, edi);
    if (CMP_EQ(MEM16(ebx), 0)) { sub_00371C14(); return; } /* je: equal / zero */

loc_00371BC0:
    edi = ebx;
    edi = edi - esi;
    if (CMP_EQ(MEM16(esi), 0)) goto loc_00371BFB; /* je: equal / zero */

loc_00371BCA:
    /* nop */

loc_00371BD0:
    eax = 0; /* xor self */
    SET_LO16(eax, MEM16(edi + esi));
    PUSH32(esp, eax);
    PUSH32(esp, 0); fn_0009EEE8_towupper(); /* call 0x0009EEE8 */

loc_00371BDC:
    ecx = 0; /* xor self */
    SET_LO16(ecx, MEM16(esi));
    SET_LO16(ebp, LO16(eax));
    PUSH32(esp, ecx);
    PUSH32(esp, 0); fn_0009EEE8_towupper(); /* call 0x0009EEE8 */

loc_00371BEA:
    esp = esp + 8;
    if (CMP_NE(LO16(ebp), LO16(eax))) { sub_00371C02(); return; } /* jne: not equal / not zero */

loc_00371BF2:
    esi = esi + 2;
    if (CMP_NE(MEM16(esi), 0)) goto loc_00371BD0; /* jne: not equal / not zero */

loc_00371BFB:
    POP32(esp, edi);
    POP32(esp, ebp);
    POP32(esp, esi);
    eax = ebx;
    POP32(esp, ebx);
    esp += 4; return; /* ret */

}

/* Fallback for unresolved generated target 0x00371BC4. */
void sub_00371BC4(void)
{
    recomp_missing_target(0x00371BC4u);
}

/**
 * sub_00371C02
 * Original: 0x00371C02 - 0x00371C14 (18 bytes, 6 insns)
 * CC: cdecl, 0 params, returns int_or_void
 * Frame: fpo_leaf
 */
void sub_00371C02(void)
{
    uint32_t ebp;
    int _flags = 0; /* fallback flag var */
    ebp = g_seh_ebp; /* fpo_leaf: inherit caller's frame */

loc_00371C02:
    ebx = ebx + 2;
    edi = edi + 2;
    if (CMP_EQ(MEM16(ebx), 0)) { sub_00371C14(); return; } /* je: equal / zero */

loc_00371C0E:
    esi = MEM32(esp + 0x14);
    g_seh_ebp = ebp; sub_00371BC4(); return; /* tail jmp 0x00371BC4 */

}

/**
 * sub_00371C14
 * Original: 0x00371C14 - 0x00371C1B (7 bytes, 6 insns)
 * CC: cdecl, 0 params, returns int_or_void
 * Frame: fpo_leaf
 */
void sub_00371C14(void)
{
    uint32_t ebp;
    ebp = g_seh_ebp; /* fpo_leaf: inherit caller's frame */

loc_00371C14:
    POP32(esp, edi);
    POP32(esp, ebp);
    POP32(esp, esi);
    eax = 0; /* xor self */
    POP32(esp, ebx);
    esp += 4; return; /* ret */

}

/**
 * fn_00371C20_stristr
 * Symbol: ?stristr@@YAPBDPBD0@Z
 * Original: 0x00371C20 - 0x00371C2F (15 bytes, 9 insns)
 * CC: cdecl, 0 params, returns int_or_void
 * Frame: fpo_leaf
 */
void fn_00371C20_stristr(void)
{
    int _flags = 0; /* fallback flag var */

    eax = zeroutils_stristr_host(eax, MEM32(esp + 4));
    esp += 4; return;

loc_00371C20:
    PUSH32(esp, ebx);
    PUSH32(esp, esi);
    esi = MEM32(esp + 0xC);
    (void)0; /* test esi, esi - flags set for next jcc */
    ebx = eax;
    if (!zeroutils_va_is_valid(esi) || !zeroutils_va_is_valid(ebx)) {
        eax = 0;
        POP32(esp, esi);
        POP32(esp, ebx);
        esp += 4; return;
    }
    if (TEST_NZ(esi, esi)) { sub_00371C2F(); return; } /* jne: not equal / not zero */

loc_00371C2C:
    POP32(esp, esi);
    POP32(esp, ebx);
    esp += 4; return; /* ret */

}

/**
 * sub_00371C2F
 * Original: 0x00371C2F - 0x00371C38 (9 bytes, 6 insns)
 * CC: cdecl, 0 params, returns int_or_void
 * Frame: fpo_leaf
 */
void sub_00371C2F(void)
{
    int _flags = 0; /* fallback flag var */

loc_00371C2F:
    if (TEST_NZ(ebx, ebx)) { sub_00371C38(); return; } /* jne: not equal / not zero */

loc_00371C33:
    POP32(esp, esi);
    eax = 0; /* xor self */
    POP32(esp, ebx);
    esp += 4; return; /* ret */

}

/**
 * sub_00371C38
 * Original: 0x00371C38 - 0x00371C7B (67 bytes, 30 insns)
 * CC: cdecl, 0 params, returns int_or_void
 * Frame: fpo_leaf
 */
void sub_00371C38(void)
{
    uint32_t ebp;
    int _flags = 0; /* fallback flag var */
    ebp = g_seh_ebp; /* fpo_leaf: inherit caller's frame */

loc_00371C38:
    (void)0; /* cmp MEM8(ebx), 0 - flags set for next jcc */
    PUSH32(esp, ebp);
    PUSH32(esp, edi);
    if (CMP_EQ(MEM8(ebx), 0)) { sub_00371C8A(); return; } /* je: equal / zero */

loc_00371C3F:
    edi = ebx;
    edi = edi - esi;
    if (CMP_EQ(MEM8(esi), 0)) goto loc_00371C74; /* je: equal / zero */

loc_00371C48:
    goto loc_00371C50;

    /* nop */

loc_00371C50:
    eax = (uint32_t)(int32_t)SMEM8(edi + esi);
    PUSH32(esp, eax);
    PUSH32(esp, 0); fn_0009CE2D_toupper(); /* call 0x0009CE2D */

loc_00371C5A:
    ecx = (uint32_t)(int32_t)SMEM8(esi);
    PUSH32(esp, ecx);
    ebp = eax;
    PUSH32(esp, 0); fn_0009CE2D_toupper(); /* call 0x0009CE2D */

loc_00371C65:
    esp = esp + 8;
    if (CMP_NE(ebp, eax)) { sub_00371C7B(); return; } /* jne: not equal / not zero */

loc_00371C6C:
    SET_LO8(eax, MEM8(esi + 1));
    esi++;
    if (TEST_NZ(LO8(eax), LO8(eax))) goto loc_00371C50; /* jne: not equal / not zero */

loc_00371C74:
    POP32(esp, edi);
    POP32(esp, ebp);
    POP32(esp, esi);
    eax = ebx;
    POP32(esp, ebx);
    esp += 4; return; /* ret */

}

/* Fallback for unresolved generated target 0x00371C43. */
void sub_00371C43(void)
{
    recomp_missing_target(0x00371C43u);
}

/**
 * sub_00371C7B
 * Original: 0x00371C7B - 0x00371C8A (15 bytes, 7 insns)
 * CC: cdecl, 0 params, returns int_or_void
 * Frame: fpo_leaf
 */
void sub_00371C7B(void)
{
    uint32_t ebp;
    int _flags = 0; /* fallback flag var */
    ebp = g_seh_ebp; /* fpo_leaf: inherit caller's frame */

loc_00371C7B:
    SET_LO8(eax, MEM8(ebx + 1));
    ebx++;
    edi++;
    if (TEST_Z(LO8(eax), LO8(eax))) { sub_00371C8A(); return; } /* je: equal / zero */

loc_00371C84:
    esi = MEM32(esp + 0x14);
    g_seh_ebp = ebp; sub_00371C43(); return; /* tail jmp 0x00371C43 */

}

/**
 * sub_00371C8A
 * Original: 0x00371C8A - 0x00371C91 (7 bytes, 6 insns)
 * CC: cdecl, 0 params, returns int_or_void
 * Frame: fpo_leaf
 */
void sub_00371C8A(void)
{
    uint32_t ebp;
    ebp = g_seh_ebp; /* fpo_leaf: inherit caller's frame */

loc_00371C8A:
    POP32(esp, edi);
    POP32(esp, ebp);
    POP32(esp, esi);
    eax = 0; /* xor self */
    POP32(esp, ebx);
    esp += 4; return; /* ret */

}
